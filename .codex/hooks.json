{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/yonatanlivshits/Downloads/grant-finder && git add -A && { git diff --cached --quiet || { git commit -m \"auto-save $(date +%Y-%m-%dT%H:%M:%S)\" && git push; }; } 2>/dev/null || true",
            "statusMessage": "Committing and pushing changes..."
          }
        ]
      }
    ]
  }
}
